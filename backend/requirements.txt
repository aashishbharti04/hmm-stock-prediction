# Core API
fastapi==0.115.6
uvicorn[standard]==0.49.0
pydantic==2.10.4
pydantic-settings==2.14.1

# Data & modeling
numpy>=1.26,<3
pandas>=2.3.3
scikit-learn>=1.4
hmmlearn>=0.3.3
yfinance>=0.2.50

# Observability
prometheus-client>=0.21

# Utilities
python-dotenv>=1.1.1
httpx>=0.27

# Optional: shared cache. Uncomment (or `pip install redis`) and set REDIS_URL
# to use Redis instead of the built-in in-memory cache.
# redis>=5.0
